[project]
name = "threeplugpro"
version = "0.1.0"
description = "3plug-pro CLI for Triotek platform setup and governance tasks"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
3plug-pro = "threeplugpro.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
